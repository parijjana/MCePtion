[project]
name = "example-memory-server"
version = "0.1.0"
description = "Reference local stdio MCP server example."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0,<2.0",
    "pydantic>=2.0,<3.0",
]

[tool.uv]
package = true
