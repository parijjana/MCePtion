# Guidelines

This directory contains guidance and reference examples exposed by the meta MCP server.

The guidelines are intended for future agents that need explicit instructions for building or modifying MCP servers in this environment.

The default guidance is:

```text
Prefer local stdio command-per-client servers unless a shared long-running process is genuinely required.
```
