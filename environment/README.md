# Environment Replication

This directory is reserved for future environment replication manifests.

Version 1 does not use this directory to clone, pull, push, or manage child server repositories.

Future files may include:

```text
servers.yaml
servers.lock.yaml
```

The goal will be to reproduce the same local hub plus installed child server set on another machine while keeping each child server independently usable.
